services:
  llama.cpp:
    build: ./llama.cpp
    restart: unless-stopped
    gpus: all
    ports:
      - "8000:8000"
    volumes:
      - ./gguf:/gguf:ro
    command: >
      -m /gguf/${GGUF_FILE}
      --mmproj /gguf/${MMPROJ_FILE}
      -a Qwen3.8-Flash-Next
      -c ${CTX_SIZE}
